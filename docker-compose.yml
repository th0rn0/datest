version: '2'
services:
  nginx:
    build: ./docker/nginx
    ports:
#      - '8888:80'
      - '80:80'
    depends_on:
      - php
    volumes:
      - .:/var/www
  php:
    build: ./docker/php
    volumes:
      - .:/var/www
    working_dir: /var/www